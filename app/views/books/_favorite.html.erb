<% if user_signed_in? %>
  <%= form_for current_favorites, remote: true do |f| %>
    <button type="submit" class="btn btn-default btn-warning"><%= t ".favorites" %>
    </button>
  <% end %>
<% end %>

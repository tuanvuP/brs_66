<%= form_for unfavorite, remote: true do |f| %>
  <div><%= hidden_field_tag :book_id, @book.id %></div>
  <div><%= hidden_field_tag :user_id, current_user.id %></div>
  <button type="submit" class="btn btn-default"> <%= t ".favorites" %>
  </button>
<% end %>

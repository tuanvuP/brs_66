<% provide :title, t(".title") %>
<div class="row">
  <% @books.each do |book| %>
    <div class="col-md-3">
      <%= image_tag book.image, class: "book" %>
      <div class="content-book">
        <%= link_to book.name, "#" %>
        <p class="price"><%= book.price %> <%= t ".$" %></p>
      </div>
    </div>
  <% end %>
</div>

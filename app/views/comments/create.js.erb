<% if @comment.parent? %>
  $('.comment').prepend('<%= j render @comment %>');
  $('textarea').val('')
<% else  %>
  $('#reply-form-<%= j @comment.parent_id.to_s %>').before('<%= j render @comment %>');
  $('#content-<%= j @comment.parent_id.to_s %>').val('')
<% end %>
